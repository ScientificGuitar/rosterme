# Local-dev overlay: publish Postgres to the host loopback so a
# host-run backend (dotnet run, Host=localhost:5432) can reach it.
# Usage: docker compose -f compose.yaml -f compose.dev.yaml up -d db
services:
  db:
    ports:
      - "127.0.0.1:5432:5432"
